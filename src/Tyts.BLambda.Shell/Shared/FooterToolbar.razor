@using Tyts.BLambda.Shell.Pages.Settings

@attribute [Slots.FooterToolbarSlot(Order = int.MinValue)]

@inject IFlyoutService Flayout
@inject INavMenuService NavMenu
@code {
    bool collapsed => !NavMenu.IsExpanded;
}

<FluentButton Appearance="Appearance.Stealth" @onclick="@(()=>Flayout.LeftSidebar.Open("Sidebar Setup", null!))">
    <FluentIcon Value="@(new Icons.Regular.Size24.MoreHorizontal())" />
</FluentButton>

<FluentButton slot="end" Appearance="Appearance.Stealth" @onclick="@(()=> Flayout.LeftSidebar.Open("Settings", typeof(Settings)))">
    <FluentIcon Value="@(new Icons.Regular.Size24.Settings())" />
</FluentButton>